<template>
  <div class="banderole">
    <div class="banderole-track">
      <div class="banderole-container">
        <div class="banderole-content" v-for="(partener, index) in parteners" :key="index">
          <NuxtImg :src="partener.logo" :alt="partener.image_alt" width="150" />
        </div>
      </div>
      <!-- Clone pour effet infini -->
      <div class="banderole-container">
        <div class="banderole-content" v-for="(partener, index) in parteners" :key="'clone-' + index">
          <NuxtImg :src="partener.logo" :alt="partener.image_alt" width="150" />
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from "vue";
import parteners from "~/data/parteners";
</script>


<style scoped lang="scss">
.banderole {
  overflow: hidden;
  width: 100%;
  background-color: #f8f8f8;
  padding: 20px 0 60px 0;
}

.banderole-track {
  display: flex;
  width: 200%; /* Pour l'effet infini */
  animation: marquee 15s linear infinite;
}

.banderole-container {
  display: flex;
  align-items: center;
  gap: 30px;
  flex-shrink: 0; /* Empêche la réduction */
  white-space: nowrap;
}

.banderole-content {
  width: 150px;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: #ffffff;
  padding: 10px;
  border-radius: 10px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

/* Animation pour le défilement */
@keyframes marquee {
  from {
    transform: translateX(0);
  }
  to {
    transform: translateX(-50%);
  }
}
</style>
